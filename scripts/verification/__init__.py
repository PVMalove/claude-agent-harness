"""Проверки `scripts/verify.py`, разложенные по ответственностям.

`process` запускает команды и стадии, `text_checks` проверяет содержимое файлов, `docs_sync` следит
за согласованностью документации с каталогом capability, `vendor_pin` — за закреплённым snapshot
вендора, `instructions` — за стабильностью инструкций, отправляемых с каждым dispatch.
"""
