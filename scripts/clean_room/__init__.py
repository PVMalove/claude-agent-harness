"""Сценарии end-to-end clean-room проверки, разложенные по модулям.

`scripts/test_clean_room.py` запускает сценарии по порядку из `SCENARIOS` с общим контекстом:
каждый модуль экспортирует `run(ctx)`, читает из `ctx` только объявленные в docstring входы и
записывает в него значения, нужные следующим сценариям. `support` содержит общие помощники.
"""
