# Python tooling used to develop and verify this repository.
#
# Keep these direct dependencies in sync with [project.optional-dependencies].dev
# in pyproject.toml. uv.lock records the resolved transitive dependency graph.
coverage==7.16.1
mypy==2.3.1
pytest==9.1.1
pytest-cov==7.1.0
pytest-xdist==3.8.0
ruff==0.8.6
