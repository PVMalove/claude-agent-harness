{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "backend orchestration project config",
  "description": "Project-owned provider profiles and role assignment plans for the optional backend-orchestration capability. This contract is separate from .harness/project.json and does not contain credentials.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "provider_profiles",
    "assignment_plans",
    "backend_zones",
    "concurrency_budget",
    "verification_commands"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Editor-facing pointer to this schema; the harness does not read it to resolve the config."
    },
    "provider_profiles": {
      "type": "object",
      "description": "Named project-owned provider profiles. Profile IDs are referenced by ordered assignment plans.",
      "additionalProperties": {
        "$ref": "#/definitions/providerProfile"
      }
    },
    "assignment_plans": {
      "type": "object",
      "description": "Role plans with an explicit runtime-specific profile, model, and reasoning effort.",
      "additionalProperties": {
        "$ref": "#/definitions/assignmentPlan"
      }
    },
    "backend_zones": {
      "type": "object",
      "description": "Named write/read boundaries referenced by assignment plans.",
      "additionalProperties": {
        "$ref": "#/definitions/backendZone"
      }
    },
    "concurrency_budget": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of coordinator-approved active batches."
    },
    "verification_commands": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Project commands a coordinator requires as orchestration proof."
    },
    "developer_verification_commands": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Optional focused commands for a developer work dispatch. When omitted, verification_commands remains the backward-compatible developer and clean-room QA list."
    },
    "test_path_patterns": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Optional fnmatch-style glob patterns identifying this project's test files, used to gate delta-review eligibility for a test-only fix after a code-review Warning. Defaults to a Python/pytest convention (tests/**, **/tests/**, **/test_*.py, **/*_test.py) when omitted."
    },
    "approval_policy": {
      "enum": ["manual_all", "milestone", "low_risk"],
      "default": "manual_all",
      "description": "Human approval cadence. manual_all approves every transition; milestone keeps plan, risk/scope, QA and publish gates; low_risk is restricted to explicitly approved low-risk zones."
    },
    "low_risk_zones": {
      "type": "array",
      "items": {"type": "string", "minLength": 1},
      "description": "Explicitly pre-approved zones eligible for low_risk continuation; omitted means no zone is eligible."
    },
    "worker_attestation_required": {
      "type": "boolean",
      "default": true,
      "description": "Require each runtime worker to attest its canonical Git worktree, branch and pinned commit before a completion report is accepted. New projects should keep this enabled; omitted keeps legacy projects compatible."
    },
    "communication_policy": {
      "type": "object",
      "additionalProperties": false,
      "default": {
        "agent_to_agent_language": "en",
        "coordinator_report_language": "ru"
      },
      "required": ["agent_to_agent_language", "coordinator_report_language"],
      "properties": {
        "agent_to_agent_language": {
          "const": "en",
          "description": "Language for agent-to-agent handoffs, state evidence and internal protocol text."
        },
        "coordinator_report_language": {
          "const": "ru",
          "description": "Language for completion reports and summaries addressed to the coordinator."
        }
      }
    },
    "adaptive_continuation_policy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional thresholds a planned-trigger `dispatch resume` continuation must meet. Any field left out keeps the coordinator's documented default.",
      "properties": {
        "context_limit": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum --measured-value for a context-limit planned trigger. Default 150000."
        },
        "tdd_cycle_count": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum --measured-value for a tdd-cycles planned trigger. Default 3."
        },
        "failure_log_bytes": {
          "type": "integer",
          "minimum": 1,
          "description": "Minimum --measured-value for a failure-log planned trigger. Default 20000."
        }
      }
    },
    "context_package_policy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Token-aware Context Package admission limits. The package must fit within the configured context window after reserving prompt headroom.",
      "properties": {
        "max_tokens": {"type": "integer", "minimum": 1, "default": 80000},
        "context_window_tokens": {"type": "integer", "minimum": 1, "default": 150000},
        "reserved_prompt_tokens": {"type": "integer", "minimum": 1, "default": 20000}
      }
    },
    "continuation_policy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Maximum worker-session continuations per dispatch, including provider rate-limit resumes.",
      "properties": {
        "max_continuations": {"type": "integer", "minimum": 1, "default": 2},
        "max_rate_limit_resumes": {"type": "integer", "minimum": 1, "default": 1}
      }
    },
    "retry_policy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Maximum new developer attempts after review or QA asks for a retry.",
      "properties": {
        "max_developer_retries": {"type": "integer", "minimum": 0, "default": 1}
      }
    },
    "preflight_policy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Admission limits checked before a batch is created. Oversized work must be split before any model dispatch.",
      "properties": {
        "require_estimates": {"type": "boolean", "default": true},
        "max_definition_of_done_items": {"type": "integer", "minimum": 1, "default": 5},
        "max_dependencies": {"type": "integer", "minimum": 1, "default": 3},
        "max_expected_files": {"type": "integer", "minimum": 1, "default": 12},
        "max_expected_services": {"type": "integer", "minimum": 1, "default": 1},
        "max_expected_changed_lines": {"type": "integer", "minimum": 1, "default": 800},
        "max_expected_context_tokens": {"type": "integer", "minimum": 1, "default": 80000}
      }
    }
  },
  "definitions": {
    "providerProfile": {
      "type": "object",
      "additionalProperties": false,
      "required": ["capabilities", "fallback", "known_limitations"],
      "properties": {
        "capabilities": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Capabilities this profile can satisfy; credentials are never stored here."
        },
        "agent": {
          "type": "string",
          "minLength": 1,
          "description": "Orca agent identifier selected by this project-owned profile."
        },
        "fallback": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Ordered fallback provider profile IDs."
        },
        "known_limitations": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "assignmentPlan": {
      "type": "object",
      "additionalProperties": false,
      "required": ["zone", "runtimes"],
      "properties": {
        "zone": {
          "type": "string",
          "minLength": 1
        },
        "write_paths": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Optional narrower write boundary for this role. It must remain inside the assigned zone."
        },
        "transport": {
          "enum": ["orca", "in-process"],
          "default": "in-process",
          "description": "How this role is executed: an explicitly selected Orca-dispatched isolated worker, or an in-process subagent of the coordinator session. Both consume the same immutable brief and must pass the model self-report."
        },
        "default_runtime": {
          "type": "string",
          "minLength": 1,
          "description": "Project-owned default used only when this role declares more than one runtime. Without it callers must pass --runtime."
        },
        "runtimes": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "$ref": "#/definitions/runtimeAssignment"
          },
          "description": "Named runtime assignments, such as codex or claude."
        }
      }
    },
    "runtimeAssignment": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profiles", "model", "effort"],
      "properties": {
        "profiles": {
          "type": "array",
          "minItems": 1,
          "items": {"type": "string", "minLength": 1}
        },
        "model": {"type": "string", "minLength": 1},
        "effort": {"type": "string", "minLength": 1}
      }
    },
    "backendZone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["paths"],
      "properties": {
        "paths": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
